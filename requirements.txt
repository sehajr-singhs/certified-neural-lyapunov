# Pinned to what was verified working on Windows 11, CPU-only, Anaconda Python 3.13.9.
# Record the Python version: 3.13.9. No GPU was used; all timings are CPU wall-clock.

numpy==2.3.5
scipy==1.16.3
matplotlib==3.10.6
torch==2.13.0            # installed from https://download.pytorch.org/whl/cpu

# Verifier stack: install from GitHub main, NOT PyPI.
# PyPI auto_LiRPA is stuck at 0.3 and lacks JacobianOP + GenBaB (sin/cos/ELU
# branching) which this project depends on. See NOTES.md.
#   pip install git+https://github.com/Verified-Intelligence/auto_LiRPA.git
#   git clone https://github.com/Verified-Intelligence/alpha-beta-CROWN.git
# The exact commit hashes that verified here are recorded in NOTES.md once pinned.

# Set KMP_DUPLICATE_LIB_OK=TRUE before importing torch on Anaconda (OpenMP clash).
